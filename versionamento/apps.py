from django.apps import AppConfig


class VersionamentoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "versionamento"
    verbose_name = "Versionamento normativo"
